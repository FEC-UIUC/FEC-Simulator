#from fecsim import orderimport jsonimport sysdef main():	global securities, parameters	globals = Object()	initialize(globals)		_securities = json.loads(sys.argv[1])	for s in _securities:		if not s in securities:			securities.append(s)				_parameters = json.loads(sys.argv[2])	for p in _parameters:		parameters[p] = _parameters[p]			handle_data(globals, data)if __name__ == "__main__":	main()